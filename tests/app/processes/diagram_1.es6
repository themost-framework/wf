export function Start(data, done) {
    return done(data);
}

export function User_Task(data, done) {
    return done(data);
}

export function User_TaskDone(data, done) {
    return done(data);
}

export function End(data, done) {
    return done(data);
}